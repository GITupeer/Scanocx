diff --git a/node_modules/expo-mlkit-ocr/android/src/main/java/expo/modules/mlkitocr/ExpoMlkitOcrModule.kt b/node_modules/expo-mlkit-ocr/android/src/main/java/expo/modules/mlkitocr/ExpoMlkitOcrModule.kt
index 4d4e7d5..315871f 100644
--- a/node_modules/expo-mlkit-ocr/android/src/main/java/expo/modules/mlkitocr/ExpoMlkitOcrModule.kt
+++ b/node_modules/expo-mlkit-ocr/android/src/main/java/expo/modules/mlkitocr/ExpoMlkitOcrModule.kt
@@ -41,6 +41,7 @@ class ExpoMlkitOcrModule : Module() {
                     "elements" to line.elements.map { element ->
                       mapOf(
                         "text" to element.text,
+                        "confidence" to element.confidence.toDouble(),
                         "boundingBox" to (element.boundingBox?.let {
                           mapOf("x" to it.left, "y" to it.top, "width" to it.width(), "height" to it.height())
                         } ?: emptyMap<String, Int>()),
diff --git a/node_modules/expo-mlkit-ocr/build/ExpoMlkitOcr.types.d.ts b/node_modules/expo-mlkit-ocr/build/ExpoMlkitOcr.types.d.ts
index 9ac50cf..171aeb5 100644
--- a/node_modules/expo-mlkit-ocr/build/ExpoMlkitOcr.types.d.ts
+++ b/node_modules/expo-mlkit-ocr/build/ExpoMlkitOcr.types.d.ts
@@ -1,5 +1,7 @@
 export type TextElement = {
     text: string;
+    /** 0–1 gdy silnik OCR zwraca pewność; brak pola = niedostępne. */
+    confidence?: number;
     boundingBox: {
         x: number;
         y: number;
diff --git a/node_modules/expo-mlkit-ocr/ios/ExpoMlkitOcrModule.swift b/node_modules/expo-mlkit-ocr/ios/ExpoMlkitOcrModule.swift
index b8efaa2..732b508 100644
--- a/node_modules/expo-mlkit-ocr/ios/ExpoMlkitOcrModule.swift
+++ b/node_modules/expo-mlkit-ocr/ios/ExpoMlkitOcrModule.swift
@@ -116,6 +116,7 @@ public class ExpoMlkitOcrModule: Module {
             for element in line.elements {
               let elementDict: [String: Any] = [
                 "text": element.text,
+                "confidence": Double(element.confidence),
                 "boundingBox": [
                   "x": element.frame.origin.x,
                   "y": element.frame.origin.y,
@@ -170,6 +171,7 @@ public class ExpoMlkitOcrModule: Module {
           guard let top = obs.topCandidates(1).first else { continue }
 
           let lineText = top.string
+          let lineConfidence = Double(top.confidence)
           fullTextParts.append(lineText)
 
           let lineRectNorm = obs.boundingBox
@@ -190,12 +192,14 @@ public class ExpoMlkitOcrModule: Module {
             if let wordRect = try? top.boundingBox(for: range) {
               elements.append([
                 "text": word,
+                "confidence": lineConfidence,
                 "boundingBox": denormalizeVisionRect(wordRect.boundingBox, imageSize: imageSize)
               ])
             } else {
               // Fallback to line bounding box if Vision can't provide per-word boxes.
               elements.append([
                 "text": word,
+                "confidence": lineConfidence,
                 "boundingBox": denormalizeVisionRect(lineRectNorm, imageSize: imageSize)
               ])
             }
